agents:
  lucidia:
    model: Lucidia
    role: recursive-core
    locality: local

  alice:
    model: Alice
    role: infra-gateway
    locality: local

  octavia:
    model: Octavia
    role: inference-queue
    locality: local

  aria:
    model: Aria
    role: filesystem-state
    locality: local

  anastasia:
    model: Anastasia
    role: security-audit
    locality: local

humans:
  operator:
    name: Alexa
    authority: root

rules:
  execution: explicit
  defaults: deny
  logging: append-only
