{
  "name": "@blackroad/edge-blackroadio",
  "version": "1.0.0",
  "scripts": {
    "dev": "npx serve .",
    "build": "echo 'Static site - no build needed'",
    "deploy": "wrangler pages deploy . --project-name=edge-blackroadio"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
