# [TODO] — 2026-04-24

- Manual in-browser QA for `blackroad.io/home/` and at least 2 other root domains; capture and review screenshots (headless tools blocked).
- Verify that each root domain’s `/home/` header title matches `/api/sites` registry (domain identity consistency).
- Add additional root domains to `src/blackroad/sites.ts` as they come online (keep `/api/sites` as truth).
