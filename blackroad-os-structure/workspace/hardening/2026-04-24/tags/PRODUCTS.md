# [PRODUCTS] — 2026-04-24

- Product registry endpoint: `GET /api/products` (served by `blackroad-grayscale`).
- Product status truth captured in `matrices/PRODUCT_STATUS.md` (many products still `needs-review`; RoadTrip API is currently `broken`).
