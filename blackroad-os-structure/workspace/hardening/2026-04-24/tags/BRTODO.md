# [BRTODO] — 2026-04-24

1. Fix `roadtrip.blackroad.io` D1 schema error (`visibility` missing) or migrate to shared registry-backed APIs (`BR-API-001`).
2. Standardize RoadTrip product surface to black-and-white base CSS (`BR-VIS-002`).
3. Restore local source for `oneway-blackroad` worker (`/Applications/BlackRoad/apps/oneway-blackroad` is empty).
4. Resolve Visual QA tooling blockers (Cloudflare headless WAF + local Swift toolchain mismatch) (`BR-VIS-001`, `BR-VIS-003`).
