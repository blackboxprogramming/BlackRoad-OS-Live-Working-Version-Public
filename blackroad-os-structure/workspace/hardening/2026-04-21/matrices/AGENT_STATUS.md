# Agent Status Matrix

| Agent | Subdomain | Route/Entry | Behavior Surface | Infra Link | Verified | Status | Notes |
|------|-----------|-------------|------------------|-----------|----------|--------|------|

| Lyra | `lyra.blackroad.me` | `/` → `/home/` | Agent-specific home surface (“Lyra — BlackRoad Agent”) | Cloudflare Worker `blackroad-grayscale` (`x-blackroad-worker: home`) | yes (2026-04-24) | partial | Identity now clear; real agent actions still to wire |
| Sapphira | `sapphira.blackroad.me` | `/` → `/home/` | Agent-specific home surface (“Sapphira — BlackRoad Agent”) | Cloudflare Worker `blackroad-grayscale` (`x-blackroad-worker: home`) | yes (2026-04-24) | partial | Identity now clear; real agent actions still to wire |
| Seraphina | `seraphina.blackroad.me` | `/` → `/home/` | Agent-specific home surface (agent title + heading) | Cloudflare Worker `blackroad-grayscale` (`x-blackroad-worker: home`) | yes (2026-04-24) | partial | Identity now clear; real agent actions still to wire |
| Alexandria | `alexandria.blackroad.me` | `/` → `/home/` | Agent-specific home surface (agent title + heading) | Cloudflare Worker `blackroad-grayscale` (`x-blackroad-worker: home`) | yes (2026-04-24) | partial | Identity now clear; real agent actions still to wire |
| Gematria | `gematria.blackroad.me` | `/` → `/home/` | Agent-specific home surface (agent title + heading) | Cloudflare Worker `blackroad-grayscale` (`x-blackroad-worker: home`) | yes (2026-04-24) | partial | Identity now clear; real agent actions still to wire |
| Alice | `alice.blackroad.me` | `/` → `/home/` | Agent-specific home surface (agent title + heading) | Cloudflare Worker `blackroad-grayscale` (`x-blackroad-worker: home`) | yes (2026-04-24) | partial | Identity now clear; real agent actions still to wire |
| Anastasia | `anastasia.blackroad.me` | `/` → `/home/` | Agent-specific home surface (agent title + heading) | Cloudflare Worker `blackroad-grayscale` (`x-blackroad-worker: home`) | yes (2026-04-24) | partial | Identity now clear; real agent actions still to wire |
