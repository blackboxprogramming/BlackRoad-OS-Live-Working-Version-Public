// ── RENDER TICKER ──
function renderTicker(){
  var el=document.getElementById('ticker-scroll'),all=S.ticker.concat(S.ticker);
  el.innerHTML=all.map(function(t){return '<span class="ticker-item">'+t+'</span>'}).join('');
}

